{
  "name": "tilawa",
  "version": "1.0.0",
  "private": true,
  "license": "0BSD",
  "description": "Tilawa: a Telegram bot that delivers a daily Quran wird (one page by default, up to a juz) to a channel and to individual users.",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @tilawa/telegram dev",
    "build": "pnpm --filter @tilawa/telegram build",
    "start": "pnpm --filter @tilawa/telegram start",
    "db:generate": "pnpm --filter @tilawa/database db:generate",
    "db:push": "pnpm --filter @tilawa/database db:push",
    "db:migrate": "pnpm --filter @tilawa/database db:migrate",
    "db:deploy": "pnpm --filter @tilawa/database db:deploy",
    "db:studio": "pnpm --filter @tilawa/database db:studio",
    "data:fetch": "pnpm --filter @tilawa/database data:fetch",
    "db:seed": "pnpm --filter @tilawa/database db:seed",
    "typecheck": "pnpm -r exec tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "pnpm -r test",
    "format": "prettier --write \"**/*.{ts,js,mjs,json,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{ts,js,mjs,json,yml,yaml}\"",
    "check": "pnpm typecheck && pnpm lint && pnpm test",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,js,mjs}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,yml,yaml}": [
      "prettier --write"
    ]
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "esbuild",
      "prisma"
    ]
  },
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "eslint": "^9.18.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.5",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2",
    "prettier": "^3.8.3",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.59.4"
  }
}
